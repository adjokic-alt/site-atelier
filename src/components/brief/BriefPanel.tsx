"use client";
import Link from "next/link";
import { Button, Card, ProgressBar } from "@/components/ui";
import { useBrief } from "@/providers/BriefProvider";
export function BriefPanel() { const { brief, strength, saved, resetBrief } = useBrief(); return <aside aria-label="Project brief summary"><Card variant="surface" className="sticky top-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.08em] text-accent">My brief</p><h2 className="mt-2 font-display text-h3 text-ink-900">{brief.style.primaryStyleId ? `${brief.style.primaryStyleId} direction` : "Your project direction"}</h2></div><span aria-live="polite" className="text-xs text-ink-500">{saved ? "Saved" : "Saving…"}</span></div><div className="mt-6"><ProgressBar current={strength.completedSections} total={strength.totalSections} label={`Brief strength: ${strength.level}`} /></div><dl className="mt-6 space-y-3 text-sm"><div className="flex justify-between gap-4"><dt className="text-ink-500">Style</dt><dd className="capitalize text-ink-900">{brief.style.primaryStyleId ?? "Not selected"}</dd></div><div className="flex justify-between gap-4"><dt className="text-ink-500">Materials</dt><dd>{brief.style.materialIdsLiked.length}</dd></div><div className="flex justify-between gap-4"><dt className="text-ink-500">Moodboard</dt><dd>{brief.visuals.moodboardItemIds.length}</dd></div></dl>{strength.suggestions.length ? <div className="mt-6 rounded-md bg-surface-sunken p-4"><h3 className="text-sm font-medium text-ink-900">Make it stronger</h3><ul className="mt-3 space-y-2">{strength.suggestions.map((item)=><li key={item.label}><Link className="focus-ring rounded-sm text-sm text-accent hover:underline" href={item.href}>+ {item.label}</Link></li>)}</ul></div>:null}<div className="mt-6 grid gap-3"><Link href="/brief" className="focus-ring inline-flex min-h-12 items-center justify-center rounded-sm bg-ink-900 px-5 text-sm font-medium text-white">Open full brief</Link><Button variant="tertiary" onClick={resetBrief}>Start over</Button></div></Card></aside>; }
