"use client";
interface Option{value:string;label:string;description?:string}
export function OptionGrid({legend,options,selected,onChange,multiple=false}:{legend:string;options:Option[];selected:string[];onChange:(values:string[])=>void;multiple?:boolean}){const toggle=(value:string)=>onChange(multiple?(selected.includes(value)?selected.filter(x=>x!==value):[...selected,value]):[value]);return <fieldset><legend className="text-sm font-medium text-ink-700">{legend}</legend><div className="mt-3 grid gap-3 sm:grid-cols-2">{options.map(option=>{const active=selected.includes(option.value);return <button key={option.value} type="button" role={multiple?"checkbox":"radio"} aria-checked={active} onClick={()=>toggle(option.value)} className={`focus-ring min-h-12 rounded-lg border p-4 text-left ${active?"border-2 border-accent bg-accent-tint":"border-border-strong bg-surface hover:bg-surface-sunken"}`}><span className="font-medium text-ink-900">{option.label}</span>{option.description?<span className="mt-1 block text-sm text-ink-500">{option.description}</span>:null}</button>})}</div></fieldset>}
